{
  "name": "myrtle-beach-v2-sample",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": { "dev": "vite --configLoader native --host 127.0.0.1", "build": "vite build --configLoader native", "preview": "vite preview --configLoader native --host 127.0.0.1", "test": "node --test tests/*.test.js" },
  "dependencies": { "react": "^19.1.0", "react-dom": "^19.1.0", "three": "^0.180.0" },
  "devDependencies": { "vite": "^7.1.0" }
}
